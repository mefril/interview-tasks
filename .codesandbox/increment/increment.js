/**
 * Implement the function below:
 *   createIncrement()
 *
 * It should return a function that, when called, always returns the
 * previous value + 1. The first call should return 1.
 *
 * Requirements:
 * - No global variables should be declared
 * - No external storage (like objects, arrays, etc.) should be used
 * - Use closures to maintain state
 *
 * Example:
 *   const increment = createIncrement();
 *   console.log(increment()); // 1
 *   console.log(increment()); // 2
 *   console.log(increment()); // 3
 */

export function createIncrement() {} 